---
name: CI

on:
  pull_request:
  merge_group:

permissions: {}

jobs:
  required-checks:
    name: CI - Required Checks
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Require checks in PR
        uses: devantler-tech/actions/require-checks-in-pr@55e9a09c265e68c601e22c44be8b9ec8d817cabb # v3.2.1
        with:
          job-results: ""
